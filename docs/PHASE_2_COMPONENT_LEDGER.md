# Phase 2 component ledger

| Component | Source | License | Runtime dependencies | Adaptation | Preview usage |
| --- | --- | --- | --- | --- | --- |
| Split Stage Title | React Bits free **Split Text**, TS + CSS variant: <https://www.reactbits.dev/text-animations/split-text>; official source: <https://github.com/DavidHDev/react-bits/tree/main/src/ts-default/TextAnimations/SplitText> | MIT + Commons Clause License Condition v1.0, copyright © 2026 David Haz: <https://github.com/DavidHDev/react-bits/blob/main/LICENSE.md> (Status: `REQUIRES APPROVAL` before commit per control document §9.4) | `gsap` and `@gsap/react`; no React Bits package. ScrollTrigger ships with GSAP. | Rebuilt as semantic word spans instead of characters; fixed heading tags only; intact `aria-label` with visual fragments `aria-hidden`; no inline styles, callback API, per-title font state, GSAP SplitText instance, or per-title ScrollTrigger. Animation ownership moves to the route's single scoped GSAP context. CSS is fully restyled to the Human Systems tokens and project accents. Reduced motion renders the static heading. | All 6 project stage titles on `/phase-2-preview` (BETTR, CardioPal, FrankenTeen, Echoes of Home, Breaking the Smartphone Mold, Playing Freedom). Hero G is untouched. |
| Preview Dock / Dock | React Bits free **Dock**, TS + CSS variant: <https://www.reactbits.dev/components/dock>; official source: <https://github.com/DavidHDev/react-bits> | MIT + Commons Clause License Condition v1.0, copyright © 2026 David Haz: <https://github.com/DavidHDev/react-bits/blob/main/LICENSE.md> (Status: `REQUIRES APPROVAL` before commit per control document §9.4) | `gsap` and `@gsap/react`; no `motion` package installed or imported. | Reimplemented cursor proximity magnification and spring physics natively using GSAP (`gsap.to` cosine-squared curve + `back.out` easing) instead of Framer Motion (`motion`). Derived typographic mono glyphs (`RL`, `WK`, `AB`, `CT`) from existing identity system rather than stock icon kits. Added active route indicator (`usePathname`), keyboard focus/blur magnification, `data-cursor-color` custom cursor opt-in, and static reduced motion fallback. | Scoped bottom navigation dock on `/phase-2-preview` layout, replacing `PreviewNavbar`. |
| Flowing Menu / FlowingMenu | React Bits free **Flowing Menu**, TS + CSS variant: <https://www.reactbits.dev/components/flowing-menu>; official source: <https://github.com/DavidHDev/react-bits> | MIT + Commons Clause License Condition v1.0, copyright © 2026 David Haz: <https://github.com/DavidHDev/react-bits/blob/main/LICENSE.md> (Status: `REQUIRES APPROVAL` before commit per control document §9.4) | `gsap` and `@gsap/react`; no external image packages or Framer Motion installed. | Replaced stock raster image tiles with bespoke SVG motif badges and brand accent token swatches (`#ff6a78`, `#82b5a5`, etc.) to uphold the image-free homepage constraint. Added keyboard focus/blur parity (`onFocus`/`onBlur`) for edge-aware marquee slide-in, `data-cursor-color` custom cursor opt-in, and static reduced motion fallback (disabling both slide-in reveals and continuous marquee loops). | Parallel work index comparison route at `/phase-2-preview/work-flowing`. |
| Motion Primitives Cursor / MotionCursor | Motion Primitives free **Cursor**, <https://motion-primitives.com/docs/cursor>; official source: <https://github.com/ibelick/motion-primitives> | MIT License, copyright © Julien Thibeaut (ibelick): <https://github.com/ibelick/motion-primitives/blob/main/package.json> (Status: Approved for production use under standard open-source MIT terms; no Commons Clause restriction or special commercial gate required) | `motion` package (`import { motion, useMotionValue, useSpring } from "motion/react"`). | Adapted Motion Primitives spring physics (`useSpring`) for smooth trailing ring motion. Retained fine-pointer checking (skips on touch devices), native cursor fallback under `prefers-reduced-motion: reduce`, interactive element hover scaling, and dynamic `data-cursor-color` accent opt-in. | Scoped custom cursor on `/phase-2-preview` layout, replacing `PreviewCursor`. |
| Decrypted Text / DecryptedText | React Bits free **Decrypted Text**, TS + CSS variant: <https://www.reactbits.dev/text-animations/decrypted-text>; official source: <https://github.com/DavidHDev/react-bits/blob/main/src/ts-default/TextAnimations/DecryptedText/DecryptedText.tsx> | MIT + Commons Clause License Condition v1.0, copyright © 2026 David Haz: <https://github.com/DavidHDev/react-bits/blob/main/LICENSE.md> (Status: `REQUIRES APPROVAL` before commit per control document §9.4) | `motion` package (`import { motion } from "motion/react"`); no external text-scramble packages. | Adapted to accept controlled `isTriggered` prop for keyboard focus and hover parity. Added accessible screen-reader hidden span (`.srOnly`) for full accessible tree integrity. Added reduced motion path resolving final text immediately with zero scramble. | Project display titles on `WorkIndexPage` (`/phase-2-preview/work`). |
| Paper Shaders Grain Gradient / GrainGradient | Paper Design free **Grain Gradient**, `@paper-design/shaders-react` package: <https://www.npmjs.com/package/@paper-design/shaders-react>; official repository: <https://github.com/paper-design/shaders> | Apache License 2.0, copyright © Paper Design: <https://github.com/paper-design/shaders/blob/main/LICENSE> (Status: `APPROVED` for production use under standard permissive Apache-2.0 terms; no Commons Clause restriction or special commercial gate required) | `@paper-design/shaders-react` & `@paper-design/shaders` (native WebGL 2 / WebGL 1 shader canvas). | Wrapped inside shared `ProjectBackground` component. Tuned for dense editorial case-study reading with quiet opacity, high softness (`0.85`), fine noise (`0.18`), and gentle ambient drift (`speed={0.14}`). Generates project-specific hex palettes dynamically from `accent` and `bgHex` tokens. Configured `prefers-reduced-motion: reduce` to set `speed={0}`, immediately freezing to a static WebGL frame and halting the `requestAnimationFrame` loop completely. | Shared case-study ambient background in `ProjectBackground`, mounted across project case-study pages (`/projects/[slug]`). |

Verified against the official React Bits site, Motion Primitives repository, and Paper Design repository. The Commons Clause for React Bits components permits use and modification as part of an application or website but prohibits selling or redistributing the components themselves. Paper Design components are standard permissive Apache-2.0. This portfolio adaptation is application code, not a component redistribution. Final licence approval is tracked per `docs/PORTFOLIO_PHASE_2_CONTROL.md`.
